extends: thu.yaml
downstream:
  regions:
    mode: grid
    grid: [3, 3, 1]
    plane_mode: all
    num_regions: 27
  feature_cache:
    enabled: true
    dir: outputs/region_worldmodel_features
    batch_size: 32
    rebuild: true